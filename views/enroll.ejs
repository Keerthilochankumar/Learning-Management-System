<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="./css/styles.css">
  <meta name="csrf-token" content="<%= csrfToken %>">
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <header class="text-3xl text-center font-bold py-8 bg-gray-800 text-gray-100">
    Enrollment Details
  </header>

  <main class="container mx-auto py-8">
    <section class="flex flex-col mx-auto border border-gray-300 rounded-md p-4">
      <h2 class="text-lg font-medium mb-2">Course Information</h2>
      <ul class="list-disc space-y-2">
        <li><strong>Course Title:</strong> <%= course.coursetitle %></li>
        <li><strong>Course Content:</strong> <%= course.coursecontent %></li>
        <li><strong>Instructor:</strong> <%= user.firstname %></li>
        <li><strong>Course ID:</strong> <%= courseid %></li>
      </ul>
    </section>
    
    <form action="/enroll" method="post" class="text-center mt-4">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
      <input type="hidden" name="id" value="<%= courseid %>">
      <input type="hidden" name="userid" value="<%= userid %>">
      <button type="submit" class="inline-flex items-center px-4 py-2 bg-blue-500 hover:bg-blue-700 text-white font-bold rounded-md shadow-sm">
        Enroll Now
      </button>
    </form>
  </main>
     <ul class="list-disc space-y-4">
      <% chapters.forEach(chapter => { %>
        <a  class="flex items-center border-2 border-gray-300 hover:border-gray-500 rounded-md p-4 transition duration-200 ease-in-out">
          <div class="flex-grow">
            <h3 class="text-lg font-medium mb-2">
              <%= chapter.chname %>
            </h3>
            <p class="text-gray-700">
              <%= chapter.chcontent %>
            </p>
          </div>
          <div class="text-gray-500 font-medium">
            <%= chapter.id %>
          </div>
        </a>
      <% }); %>
    </ul>

  <script>var token = document.querySelector('meta[name="csrf-token"]').getAttribute("content");</script>
</body>
</html>
